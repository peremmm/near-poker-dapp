[package]
name = "contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
near-sdk = "5.17.3"

[dev-dependencies]
near-sdk = { version = "5.17.3", features = ["unit-testing"] }
